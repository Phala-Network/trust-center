# DStack Verifier - Environment Configuration Example
# Copy this file to .env and update with your actual values

# === Database Configuration ===
POSTGRES_DB=dstack_verifier_dev
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# === Redis Configuration ===
REDIS_PASSWORD=dev_password
REDIS_PORT=6379

# === Application Configuration ===
NODE_ENV=development
PORT=3000
APP_PORT=3000
STUDIO_PORT=4983

# === DStack Blockchain Configuration ===
DSTACK_BASE_URL=https://api.base.org
DSTACK_KMS_CONTRACT=0x...
DSTACK_APP_CONTRACT=0x...

# === Queue Configuration ===
QUEUE_NAME=verification-queue
QUEUE_CONCURRENCY=5
QUEUE_MAX_ATTEMPTS=3
QUEUE_BACKOFF_DELAY=2000

# === Development/Debugging ===
DEBUG=dstack:*
LOG_LEVEL=debug

# === External Services (Optional) ===
# Cloudflare R2 or AWS S3 compatible storage
R2_ENDPOINT=https://your-account.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=your-access-key-id
R2_SECRET_ACCESS_KEY=your-secret-access-key
R2_BUCKET=dstack-verification-results

# === TEE Verification Settings ===
# Intel DCAP-QVL settings
DCAP_QVL_PATH=/app/bin/dcap-qvl
DCAP_COLLATERAL_URL=https://api.trustedservices.intel.com/sgx/certification/v4/

# DStack measurement settings
DSTACK_MEASUREMENT_TIMEOUT=300000
DSTACK_DOCKER_PRIVILEGED=true

# === Certificate Verification Settings ===
# Certificate Transparency log URLs
CT_LOG_URL=https://crt.sh
DNS_SERVER=8.8.8.8

# === Production Overrides ===
# Uncomment and modify for production deployment
# NODE_ENV=production
# LOG_LEVEL=info
# QUEUE_CONCURRENCY=10
# POSTGRES_DB=dstack_verifier
# REDIS_PASSWORD=your_secure_redis_password